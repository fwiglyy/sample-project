name: Test Workflow

on:
  push:
    branches: [ "main" ]

jobs:
  run-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          pip install requests

      - name: Run test script
        run: |
          echo "Running tests..."
          python3 - << 'EOF'
import requests
print("Test passed: Able to import requests")
EOF
